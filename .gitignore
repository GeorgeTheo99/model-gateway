# Real provider credentials — never commit
config/config.yaml
config/config.yaml.bak-*
config/secrets/
config/*.onboard.lock

# Machine-local model catalog (per-machine; committed in error previously)
model-info.json
model-info.json.bak-*

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# macOS
.DS_Store

# Local backups
*.bak.*
